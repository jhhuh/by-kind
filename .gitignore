# Acquired / derived data — regenerated by `src/acquire.py`, never committed.
# model/weights.*.tsv IS committed (reviewable, diffable) and is not ignored here.
data/packages.jsonl
data/packages.json
data/*.br
data/categories.sqlite
data/categories.json
vendor/nixpkgs/

# Build / emit output
dist/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Nix / direnv
result
result-*
.direnv/

# Logs from test and pipeline runs (artifacts/devlog.md and skills/ ARE committed)
artifacts/logs/
