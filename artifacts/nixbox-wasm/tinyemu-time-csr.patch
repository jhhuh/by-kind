--- riscv_cpu.h.orig	2026-08-02 19:29:04.456520160 +0900
+++ riscv_cpu.h	2026-08-02 19:29:04.473520061 +0900
@@ -55,6 +55,9 @@
     uint32_t (*riscv_cpu_get_misa)(RISCVCPUState *s);
     void (*riscv_cpu_flush_tlb_write_range_ram)(RISCVCPUState *s,
                                                 uint8_t *ram_ptr, size_t ram_size);
+    void (*riscv_cpu_set_get_time)(RISCVCPUState *s,
+                                   uint64_t (*get_time)(void *opaque),
+                                   void *opaque);
 } RISCVCPUClass;
 
 typedef struct {
@@ -115,4 +118,15 @@
     c->riscv_cpu_flush_tlb_write_range_ram(s, ram_ptr, ram_size);
 }
 
+/* Supply the machine's time source so the CPU can implement the 'time' CSR
+   (0xc01 / 0xc81). Without it TinyEMU relies on M-mode firmware trapping and
+   emulating rdtime, which breaks modern guests whose vDSO reads it from U-mode. */
+static inline void riscv_cpu_set_get_time(RISCVCPUState *s,
+                                          uint64_t (*get_time)(void *opaque),
+                                          void *opaque)
+{
+    const RISCVCPUClass *c = ((RISCVCPUCommonState *)s)->class_ptr;
+    c->riscv_cpu_set_get_time(s, get_time, opaque);
+}
+
 #endif /* RISCV_CPU_H */
--- riscv_cpu_priv.h.orig	2026-08-02 19:29:04.458520148 +0900
+++ riscv_cpu_priv.h	2026-08-02 19:29:04.474520055 +0900
@@ -235,6 +235,10 @@
 
     PhysMemoryMap *mem_map;
 
+    /* machine-supplied time source for the 'time' CSR */
+    uint64_t (*get_time)(void *opaque);
+    void *get_time_opaque;
+
     TLBEntry tlb_read[TLB_SIZE];
     TLBEntry tlb_write[TLB_SIZE];
     TLBEntry tlb_code[TLB_SIZE];
--- riscv_cpu.c.orig	2026-08-02 19:29:04.459520142 +0900
+++ riscv_cpu.c	2026-08-02 19:29:04.474520055 +0900
@@ -644,8 +644,17 @@
                       MSTATUS_FS | \
                       MSTATUS_MPRV | MSTATUS_SUM | MSTATUS_MXR)
 
-/* cycle and insn counters */
-#define COUNTEREN_MASK ((1 << 0) | (1 << 2))
+/* Read the machine's time source. Falls back to the instruction counter scaled
+   the same way riscv_machine.c's rtc_get_time() does when no source is set. */
+static uint64_t glue(riscv_cpu_get_time, MAX_XLEN)(RISCVCPUState *s)
+{
+    if (s->get_time)
+        return s->get_time(s->get_time_opaque);
+    return s->insn_counter / 16;
+}
+
+/* cycle, time and insn counters (CY | TM | IR) */
+#define COUNTEREN_MASK ((1 << 0) | (1 << 1) | (1 << 2))
 
 /* return the complete mstatus with the SD bit */
 static target_ulong get_mstatus(RISCVCPUState *s, target_ulong mask)
@@ -743,6 +752,36 @@
         }
         val = (int64_t)s->insn_counter;
         break;
+    case 0xc01: /* time */
+        {
+            uint32_t counteren;
+            if (s->priv < PRV_M) {
+                if (s->priv < PRV_S)
+                    counteren = s->scounteren;
+                else
+                    counteren = s->mcounteren;
+                if (((counteren >> (csr & 0x1f)) & 1) == 0)
+                    goto invalid_csr;
+            }
+        }
+        val = (int64_t)glue(riscv_cpu_get_time, MAX_XLEN)(s);
+        break;
+    case 0xc81: /* timeh */
+        if (s->cur_xlen != 32)
+            goto invalid_csr;
+        {
+            uint32_t counteren;
+            if (s->priv < PRV_M) {
+                if (s->priv < PRV_S)
+                    counteren = s->scounteren;
+                else
+                    counteren = s->mcounteren;
+                if (((counteren >> (csr & 0x1f)) & 1) == 0)
+                    goto invalid_csr;
+            }
+        }
+        val = glue(riscv_cpu_get_time, MAX_XLEN)(s) >> 32;
+        break;
     case 0xc80: /* mcycleh */
     case 0xc82: /* minstreth */
         if (s->cur_xlen != 32)
@@ -1263,6 +1302,14 @@
     return s->insn_counter;
 }
 
+static void glue(riscv_cpu_set_get_time, MAX_XLEN)(RISCVCPUState *s,
+                                                   uint64_t (*get_time)(void *opaque),
+                                                   void *opaque)
+{
+    s->get_time = get_time;
+    s->get_time_opaque = opaque;
+}
+
 static void glue(riscv_cpu_set_mip, MAX_XLEN)(RISCVCPUState *s, uint32_t mask)
 {
     s->mip |= mask;
@@ -1343,6 +1390,7 @@
     glue(riscv_cpu_get_power_down, MAX_XLEN),
     glue(riscv_cpu_get_misa, MAX_XLEN),
     glue(riscv_cpu_flush_tlb_write_range_ram, MAX_XLEN),
+    glue(riscv_cpu_set_get_time, MAX_XLEN),
 };
 
 #if CONFIG_RISCV_MAX_XLEN == MAX_XLEN
--- riscv_machine.c.orig	2026-08-02 19:29:04.460520137 +0900
+++ riscv_machine.c	2026-08-02 19:29:15.641454547 +0900
@@ -87,6 +87,14 @@
         (ts.tv_nsec / (1000000000 / RTC_FREQ));
 }
 
+static uint64_t rtc_get_time(RISCVMachine *m);
+
+/* thunk so the CPU can read the same clock the CLINT's mtime exposes */
+static uint64_t riscv_machine_get_time(void *opaque)
+{
+    return rtc_get_time((RISCVMachine *)opaque);
+}
+
 static uint64_t rtc_get_time(RISCVMachine *m)
 {
     uint64_t val;
@@ -867,6 +875,7 @@
     s->mem_map->flush_tlb_write_range = riscv_flush_tlb_write_range;
 
     s->cpu_state = riscv_cpu_init(s->mem_map, max_xlen);
+    riscv_cpu_set_get_time(s->cpu_state, riscv_machine_get_time, s);
     if (!s->cpu_state) {
         vm_error("unsupported max_xlen=%d\n", max_xlen);
         /* XXX: should free resources */
